import equilateral from './equilateral';

test('Print a equilateral triangle with rows', () => {
    expect(equilateral(5)).toBe('Triangle Printed!');
});

test('Print a equilateral triangle with rows', () => {
    expect(equilateral(10)).toBe('Triangle Printed!');
});
